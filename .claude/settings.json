{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import json,sys,subprocess; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path',''); subprocess.run('npx prettier --write \\\"'+f+'\\\"',shell=True,cwd='C:/Users/Ahmed/tourneepro/backend',capture_output=True) if f and f.endswith('.ts') else None\" 2>/dev/null || true",
            "timeout": 15,
            "statusMessage": "Formatting TypeScript..."
          }
        ]
      }
    ]
  }
}
